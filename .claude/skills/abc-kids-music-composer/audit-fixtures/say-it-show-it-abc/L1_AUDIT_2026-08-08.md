# L1 Audio Audit — Say It! Show It! A-B-C!

Audio: `F:\MMO\Nhac\Say It! Show It! A-B-C!.mp3`
Date: 2026-08-08
Validation level: L1 generated-audio audit

## Objective measurements

- Duration: 210.0 s (3:30)
- Audio: MP3, stereo, 48 kHz, ~191 kbps
- Prompt tempo: ~92 BPM
- Measured beat estimate: ~89.29 BPM
- Integrated loudness: ~-14.7 LUFS
- True peak: ~-0.1 dBTP
- Loudness range: ~2.6 LU
- Audio-level silence below -35 dB is minimal during the song; backing music remains continuous, so retrieval-space measurement must use vocal word timing rather than whole-mix silence.

## ASR proxy method

Two temporary local ASR passes were used without supplying the reference lyric as a prompt:

- `faster-whisper tiny.en`
- `faster-whisper small.en`

ASR is an intelligibility proxy only. A repeated ASR error prioritizes human listening; it does not by itself prove a pronunciation or child-perception failure.

## Target intelligibility proxy

### Round 1

`small.en` recognized 25/26 canonical target words exactly.

The only target ambiguity was the first occurrence of:

- `X -> xylophone`: recognized as `zollophone` (word probability ~0.43).

The second `xylophone` occurrence in Round 2 was recognized correctly with much stronger probability (~0.83), which suggests a local melodic/delivery problem rather than a universally unusable target word.

### Round 2

`small.en` recognized 22/26 canonical target words exactly.

Targets needing human-listen priority:

- `P -> pig`: recognized as `Clique` with low probability (~0.28).
- `V -> van`: beginning of the phrase was recognized as `B then ...`; cue/target onset is unclear in ASR.
- `W -> whale`: letter `W` was clear, but target became `will` (~0.55 probability).
- `Y -> yo-yo`: became `Yow, yow`.

The last 6-line Round 2 `U–Z` block contains three of the four Round 2 target ambiguities (`V/W/Y`) plus the most compressed retrieval gap (`X`). This is a strong production signal to test a shorter section split next.

Overall exact target-word ASR proxy: 47/52 occurrences (~90%), with most ambiguity concentrated late in Round 2.

## Rhyme audit

Rhyme architecture is a success in this render.

Round 1 was transcribed with the expected rhyme landings essentially intact:

- sweet / feet
- tight / delight
- round / around
- hay / away
- snow / slow
- high / sky
- light / night
- round / sound
- crown / down
- bright / tight
- wide / ride
- blue / you
- drops / stops

This is materially better than the prior prose-like fixture. The rhyme pairs are not merely present on paper; the generation delivered them clearly enough for ASR to recover the landing words.

## Retrieval-gap audit

At measured tempo ~89.29 BPM, one beat is ~0.672 s.

For Round 2 target words that ASR recognized exactly, measured cue-end -> target-word-onset gaps were:

- A 0.72 s
- B 0.70 s
- C 0.54 s
- D 0.20 s
- E 0.22 s
- F 0.30 s
- G 0.22 s
- H 0.48 s
- I 0.24 s
- J 0.26 s
- K 0.44 s
- L 0.64 s
- M 0.58 s
- N 0.62 s
- O 0.66 s
- Q 0.70 s
- R 0.66 s
- S 0.64 s
- T 0.60 s
- U 0.30 s
- X 0.02 s
- Z 0.72 s

Observed range: ~0.02–0.72 s  
Observed average: ~0.475 s ~= 0.71 beat

Conclusion: **FAIL/WARN for the intended one-clear-beat retrieval design.** The generator did not realize a consistent one-beat gap. Several targets receive less than half a beat; `X -> xylophone` is effectively immediate.

Next A/B variant should author stronger separation and measure the result again rather than trusting ellipses/style prose.

## Section-density / late-song audit

No evidence of a whole A–Z section being skipped. The song completes both rounds and returns to the final hook.

However, the ASR segmentation/recognition becomes less stable late in Round 2:

- `U -> umbrella` is clear.
- `V/W/X` are merged into one ASR segment.
- `Y/Z` are merged into another segment.
- V/W/Y target identity becomes ambiguous.

Recommendation for next variant:

```text
Round 2 chunks:
A–D
E–H
I–L
M–P
Q–T
U–X
Y–Z
```

This preserves paired rhyme while following `ADD MORE SECTIONS, NOT LONGER SECTIONS`.

Round 1 may also test the same split because first-occurrence `xylophone` was the only Round 1 target ambiguity.

## Hook audit

ASR recovered the hook correctly in the intro and middle chorus. The final hook shows mild recognition degradation (`C`/`beat` become less stable in `small.en`), though `tiny.en` recovered more of that text, so this requires human listening rather than an automatic pronunciation fail.

Approximate hook spans from ASR:

- intro: ~0.00–13.02 s
- middle chorus: ~93.26–105.44 s
- final chorus: ~186.50–197.98 s

The final hook is somewhat shorter than the intro, suggesting slightly tighter final delivery. This is a monitoring point, not a hard fail.

## Mix / dynamics audit

- Integrated loudness ~-14.7 LUFS: reasonable distribution/master level.
- True peak ~-0.1 dBTP: very close to ceiling.
- LRA ~2.6 LU: low macro-dynamic contrast.

Low LRA is not automatically bad for children's music, but this render provides relatively little large-scale energy contrast between teaching, retrieval and hook sections. A future A/B can test slightly more section contrast while keeping target onsets clear.

## L1 status

| Check | Status | Evidence |
|---|---|---|
| Song completes both A–Z rounds | PASS | ASR sequence covers all sections |
| Tempo / no global rush | PASS | ~89.3 BPM; Round 1 highly intelligible |
| Rhyme landing clarity | PASS | paired end-rhymes recovered strongly |
| Round 1 target intelligibility proxy | PASS/WARN | 25/26 exact; Xylophone first occurrence ambiguous |
| Round 2 target intelligibility proxy | WARN | 22/26 exact; P/V/W/Y need listen/repair |
| Retrieval gap realization | FAIL/WARN | avg ~0.71 beat; several <0.5 beat; X ~0.02 s |
| Late 6-line retrieval section | WARN | V/W/Y ambiguity concentrated in U–Z block |
| Umbrella lexical item recognition | PASS proxy | both occurrences recognized; stress still requires human ear |
| Xylophone | WARN | R1 ambiguous, R2 clear |
| Hook text identity | PASS/WARN | intro/mid strong; final slightly compressed/less stable in one ASR model |
| Macro dynamics | WARN | LRA ~2.6 LU |
| Child memorability | NOT ESTABLISHED | requires L2/L3, not inferable from ASR |

AUDIO STATUS: **NEEDS LOCAL REPAIR / A-B VARIANT**  
RELEASE STATUS: **NOT READY**

## Next controlled variant

Change only the major failure variables:

1. Split `U–Z` into `U–X` + `Y–Z` (at least in Round 2; preferably both rounds for controlled structure symmetry).
2. Strengthen authored retrieval separation; target a clearly measurable realized gap instead of merely writing `...`.
3. Shorten/support the first `Xylophone` teaching line while preserving `X -> Xylophone` Letter-Name mapping.
4. Reinforce `P -> pig` target onset in Round 2 without breaking the `round / sound` rhyme pair.
5. Keep rhyme architecture, tempo family, voice and core arrangement otherwise locked for the A/B comparison.
