# Runtime dependencies for the Audio_Worker.
# Transcription and alignment libraries are pinned in their tasks (6.x).

# Transcription (Transcriber, task 6.7): WhisperX provides Whisper ASR plus
# wav2vec2 forced alignment for word-level timing. It is a heavy ML stack
# (pulls in torch, torchaudio, faster-whisper, pyannote-audio, ctranslate2) and
# is NOT required to import or unit-test src/transcribe.py -- the WhisperX call
# is isolated behind the lazily-imported _run_whisperx seam, and the chaining
# logic is tested with an injected fake. Install it only on machines that run
# real transcription (it may also require a matching CUDA/torch build and ffmpeg
# on PATH). Pinned to the latest stable release:
#   whisperx==3.8.6
# Left commented so `pip install -r requirements.txt` stays lightweight for the
# MVP and CI; uncomment (or `pip install whisperx==3.8.6`) to enable real runs.
# whisperx==3.8.6

# Audio analysis (Audio_Analyzer, task 7.1): librosa decodes audio (FFmpeg-backed)
# and provides beat tracking; numpy powers the pure analysis helpers; soundfile is
# librosa's default I/O backend.
numpy==2.3.5
librosa==0.10.2.post1
soundfile==0.12.1

# Testing
pytest==8.3.4
hypothesis==6.123.2
jsonschema==4.25.1  # validate lyrics.json against the shared schema (Property 4)
